// types
import { TServiceObject } from "../../types";

const mobilesData: TServiceObject = {
  service: "mobiles",
  servicePath: "mobiles",
  serviceType: "Mobile",
  data: [],
};

export default mobilesData;
