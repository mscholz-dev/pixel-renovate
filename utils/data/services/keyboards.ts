// types
import { TServiceObject } from "../../types";

const keyboardsData: TServiceObject = {
  service: "claviers",
  servicePath: "keyboards",
  serviceType: "Clavier",
  data: [],
};

export default keyboardsData;
